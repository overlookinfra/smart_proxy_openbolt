---
name: Prepare Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release (e.g. 1.2.0)'
        required: true
        type: string

permissions: {}

env:
  BUNDLE_WITH: release

jobs:
  prepare_release:
    name: Prepare Release
    runs-on: ubuntu-24.04
    if: github.repository_owner == 'overlookinfra'
    permissions:
      contents: write
      issues: read # changelog generator reads release-summary issues
      pull-requests: write
    steps:
      - name: Validate version format
        env:
          VERSION: ${{ inputs.version }}
        run: |
          if ! echo "$VERSION" | grep -qE '^[0-9]+\.[0-9]+\.[0-9]+$'; then
            echo "::error::Version '$VERSION' must be in X.Y.Z format"
            exit 1
          fi

      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 'ruby'
          bundler-cache: true

      - name: Bump version in version.rb
        env:
          VERSION: ${{ inputs.version }}
        run: |
          sed -i "s/VERSION = '.*'/VERSION = '$VERSION'/" lib/smart_proxy_openbolt/version.rb
          grep -q "VERSION = '$VERSION'" lib/smart_proxy_openbolt/version.rb || {
            echo "::error::Failed to update version in version.rb"
            exit 1
          }

      - name: Generate changelog
        env:
          CHANGELOG_GITHUB_TOKEN: ${{ github.token }}
        run: bundle exec rake changelog

      - name: Create pull request
        uses: peter-evans/create-pull-request@v8
        with:
          commit-message: "Release ${{ inputs.version }}"
          branch: "release-${{ inputs.version }}"
          delete-branch: true
          title: "Release ${{ inputs.version }}"
          labels: skip-changelog
          assignees: '${{ github.triggering_actor }}'
          body: |
            Automated release prep for version ${{ inputs.version }}.

            After merging this PR, run the **Release** workflow to create the tag and publish the gem.
