---
name: CI

on:
  pull_request: {}
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ${{ github.ref_name }}-${{ github.workflow }}
  cancel-in-progress: true

jobs:
  test:
    name: Ruby
    uses: theforeman/actions/.github/workflows/smart_proxy_plugin.yml@v0

  rubocop:
    name: RuboCop
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true
      - run: bundle exec rubocop

  acceptance-ssh:
    name: Acceptance (SSH)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true
      - name: Start proxy and SSH targets
        run: bundle exec rake acceptance:ssh:up
      - name: Run SSH acceptance tests
        run: bundle exec rake acceptance:ssh
      - name: Stop containers
        if: always()
        run: bundle exec rake acceptance:ssh:down

  tests:
    needs:
      - test
      - rubocop
      - acceptance-ssh
    runs-on: ubuntu-latest
    name: Test suite
    steps:
      - run: echo Test suite completed
